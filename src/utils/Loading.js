/*
useEffect(() => {
    getData();
  }, []);

  const getData = async (): Promise<void> => {
    await new Promise((r) => {
      setTimeout(r, 1000);
    });
    setState({
      ...state,
      loading: !state.loading,
    });
  };
*/
